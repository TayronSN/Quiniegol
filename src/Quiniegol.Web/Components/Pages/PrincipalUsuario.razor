@page "/principal-usuario"

@rendermode InteractiveServer

@using Quiniegol.Core.Utils

@inject NavigationManager Navigation

<h3>Principal - Usuario</h3>

@if (!Sesion.InicioSesion)
{
    <p>Debe iniciar sesión.</p>
}
else if (!Sesion.EsUser)
{
    <p>No tiene permisos para acceder a esta pagina, favor contactar al administrador.</p>
}
else
{
    <p>Bienvenido, @Sesion.UsuarioActual!.Nombre</p>

    <button @onclick="IrPronosticos">
        Pronosticos
    </button>

    <button @onclick="IrRanking">
        Ranking
    </button>
}

@code {
    // Navega a la sección donde el usuario puede realizar sus pronósticos
    private void IrPronosticos()
    {
        Navigation.NavigateTo("/pronosticos");
    }

    // Navega a la sección donde se muestran los puntos y posiciones
    private void IrRanking()
    {
        Navigation.NavigateTo("/ranking");
    }
}